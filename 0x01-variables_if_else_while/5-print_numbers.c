#include <stdio.h>
/**
 * main -prints single digits starting from zero of base 10.
 *
 *Return: Always 0.
 **/
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	printf("%d", number);

	printf("\n");

	return (0);
}
