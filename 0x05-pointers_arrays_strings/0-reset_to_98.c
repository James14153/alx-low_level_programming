#include "main.h"
#include "stdio.h"

/**
 * reset_to_98(int *n) - takes a pointer to an integer as n
 * parameter and updates the value it points to 98.
 *
 * Return: Always 0.
 */
void reset_to_98(int *n)
{
	int n = 402;

	*pN = NULL;
	pN = &n;

	pN = 98;
	_putchar("%d\n", n);

	return (0);
}
